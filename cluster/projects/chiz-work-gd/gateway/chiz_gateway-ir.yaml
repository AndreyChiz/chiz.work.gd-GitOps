apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: chiz-ingressroute
  namespace: chiz-work-gd
  labels:
    app: gateway
spec:
  entryPoints:
    - websecure # HTTPS
  routes:
    - match: Host(`chiz.work.gd`)
      kind: Rule
      services:
        - name: chiz-gateway-service
          port: 80
  tls:
    certResolver: letsencrypt
